# 3.2.09. `connection-info`, shared validator, and `doctor`

## Goal

Provide one complete read-only attachment validator, a stable machine schema
for SSH consumers, and detailed non-mutating human diagnosis.

## Sequence

Implement after 03.2.02–03.2.08. 03.2.09.1 and 03.2.09.2 call the shared
validator rather than defining attach compatibility themselves; the editor
frontend (03.2.11) consumes it through `connection-info`.

## Shared attachment validator

Refactor the read-only checks into a host module owned with lifecycle/runtime
state (normally `host/container-runtime.sh`, with SSH checks delegated to
`host/ssh.sh` and network checks to `host/network.sh`). It accepts the final
validated environment policy and checks, before transport:

- exact derived resource names and the exhaustive required resource set;
- a running, SSH-responsive development container;
- matching current `jailbox.config-digest` labels on every required
  resource and every other existing member of 03.2.02's digest inventory
  (both containers and plain, internal, and external project networks),
  including networks outside the requested mode;
- internally consistent container/network/home attachments and valid home-mode
  metadata;
- complete host and mounted SSH-generation material, ownership, modes, file
  types, pinned identity, and generated client configuration; and
- when filtering is enabled, the required running proxy and live network URL.

It creates, starts, stops, repairs, replaces, labels, or removes nothing.
Failures distinguish absence, stopped/unresponsive state, legacy or
inconsistent digest, structural damage, SSH damage, and proxy failure, and give
the owning `stop`/`up` or warned `--clean`/`up` recovery. Exact names,
not `jailbox.project`, identify resources.

## `connection-info` schema

`jailbox connection-info` loads and validates the complete environment
configuration, computes the current digest, invokes the shared validator, and
on success emits these fixed-order NUL-terminated records:

```text
ssh_config<TAB>ABSOLUTE_PATH<NUL>
ssh_host<TAB>HOST_ALIAS<NUL>
remote_path<TAB>ABSOLUTE_PATH<NUL>
project_id<TAB>PROJECT_ID<NUL>
proxy_url<TAB>PROXY_URL_OR_EMPTY<NUL>
```

Attachment-time digest computation uses 03.2.02's read-only Containerfile
classification before the validator compares labels. A missing explicit or
implicit selection emits the digest's `containerfile<TAB>missing` record and
leads to stale-sandbox guidance; it must not trigger an image build or the
launch-only missing-input error. `DEV_IMAGE` emits `none` without inspecting
Containerfile candidates. The exec and shell consumers use the same
attachment-time digest behavior.

Values are emitted verbatim. Readers split each NUL-delimited record on its
first TAB. Field names use the constrained grammar and cannot contain TAB, so
the split is unambiguous; values may contain any byte except NUL. Reject a
record without a separator, an invalid field name, duplicate records, a missing
required field, or a stream whose final record lacks its NUL terminator. The
five required records appear exactly once in the stated order. Future unknown
valid records may follow them and consumers ignore those records after checking
their framing and uniqueness. Consumers validate all required values as paths,
aliases, project IDs, or proxy URLs according to their owning policy.

The output is a byte stream, not line-oriented text. A shell consumer reads one
record at a time with `IFS= read -r -d ''` or first writes stdout to a file; it
never captures the complete stream in a shell variable because shell variables
cannot preserve NUL bytes.

`proxy_url` is required: live collision-fallback-aware internal URL when
filtering, empty otherwise. Do not expose allowlists, private-key paths,
known-host contents, container names, or raw configuration. jailbox alone
provides remote `NO_PROXY` and `no_proxy` in generated SSH configuration.
The jailbox release version governs this schema.

Remove `configure_proxy_env`'s name-based `http://$PROXY_NAME:8888` final
fallback: the internal network is created with `--disable-dns`, so that URL
can never resolve. When filtering is enabled and no live or derivable subnet
yields a proxy address, fail with a diagnostic instead of emitting an
unusable URL.

Add the command to `CLI_FLAGS_WITHOUT_VALUES`, `CLI_HELP`, dispatch,
`scripts/public-api-diff.sh`, usage, and the configuration-consuming preflight
class. It needs Podman, SSH, SHA-256, and project identity, but no Base64,
editor, config file, or `cksum`.

## `doctor`

Keep `doctor` configuration-independent and human-facing. It reports the
one-word status plus every derived Podman resource, runtime state, label
presence/consistency, home lifecycle metadata, SSH presence/permissions/
consistency, live network subnet, proxy state, and precise recovery. Without
current configuration it never claims a consistent recorded digest matches
current policy. Its stdout is not a stable machine schema. Every clean
recommendation warns of home and runtime-state deletion.

Doctor also absorbs the editor-agnostic connection instructions `ssh-config`
prints today, so its later removal costs no capability. These derive from
project identity and can therefore live only in core: the generated SSH
config path, the host alias, and the `Include` line for `~/.ssh/config`.
Doctor reports them in the absent and stopped states as well as the running
one, because "how would I connect to this project" has an answer before a
sandbox exists. These values are derived from project identity, not read from
disk, so doctor states whether the configuration file it names exists yet
rather than presenting a path to a missing file as though it were usable. It
does not reproduce `ssh-config`'s ready-to-paste host
block, which asserts a connection it has not validated; the block remains in
the generated config file that the reported path names. Editor-specific
spellings such as `remote.SSH.configFile` stay out of core: README documents
the setting name and points at the path doctor reports.

Doctor keeps its editor- and config-file-specific branches until 03.2.11
removes them, so no diagnosis disappears while the frontend layer is still
core-coupled. Reuse inspection primitives with the validator but do not make
doctor invoke configuration loading or mutation.

## Security invariants

All checks precede attachment and preserve read-only roots, dropped
capabilities, no-new-privileges, socket isolation, proxy-only egress, and strict
host-key state. Machine output contains connection metadata but no secret.

## Tests and documentation

- Unit-test each validator decision and prove no Podman/host-state mutation.
- Test missing explicit and implicit Containerfile selections during
  attachment-time digest computation, stale-sandbox refusal before transport,
  and `DEV_IMAGE` bypass of Containerfile discovery. Assert no image build or
  lifecycle mutation occurs, and cover the shared behavior in exec and shell.
- In runtime tests, feed every 03.2.07 adversarial state through
  `connection-info`, the 03.2.09.1/03.2.09.2 consumers, and doctor.
- Golden-test record order, required empty proxy, collision fallback, unknown
  forward-compatible fields, malformed/duplicate/missing fields, and secret
  absence.
- Cover NUL framing, spaces/TABs/newlines in values, a truncated final record,
  and an empty `proxy_url`.
- Cover doctor's connection instructions in the absent, stopped, and running
  states, asserting the reported path and alias match the generated SSH
  configuration.
- Document stable machine output separately from unstable human doctor output.

Run `tests/run portable` and `tests/run runtime`.

## Acceptance criteria

- All attachment consumers invoke one validator covering every policy-bearing
  resource and SSH/network dependency.
- This plan owns integration and verification of 03.2.02's complete-inventory
  digest gate in attachment validation, including stale networks outside the
  requested mode. Missing-Containerfile digest classification produces
  stale-sandbox refusal before transport without building or mutation;
  `DEV_IMAGE` bypasses that discovery. Exec and shell own their command-level
  integration tests in 03.2.09.1 and 03.2.09.2.
- Connection-info emits the exact required schema only after validation, with a
  live proxy URL or required empty value and no secret.
- Required records preserve every non-NUL value byte and malformed NUL framing
  is rejected.
- Doctor gives complete non-mutating recovery diagnosis without claiming a
  policy comparison it did not perform.
- Doctor reports the SSH config path, host alias, and include line in every
  lifecycle state, so removing `ssh-config` in 03.2.11 loses no capability.

## Non-goals

- Lifecycle mutation, a second validator, exposing the allowlist, or making
  doctor a stable automation interface.
