# 3.2.13. JailIDE launch, composition, and anchoring

> Deferred, September 2026. Superseded by the in-repo frontend plan
> (`03.2.11-editor-frontend-plan.md`), which carries this plan's composition
> order, connection-record parsing, and anchoring rules into a frontend layer
> that invokes the public CLI as child processes. The cross-product machinery
> (version ranges, fake-jailbox harness, embedded setup script) is deferred
> with the split. See `plans/repository-split-rationale.md`.

Full text preserved in git history:
`git show 9a04def:plans/03.2.13-jailide-launch-plan.md`.
