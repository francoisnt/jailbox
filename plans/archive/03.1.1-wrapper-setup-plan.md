# 3.1.1. Trusted wrapper setup inputs

> Deferred, September 2026. The repository split is deferred, and with it the
> generic `WRAPPER_SETUP` caller mechanism: its only planned consumer was
> JailIDE's single embedded editor setup script. Editor-server packages stay
> in core `container/setup.sh`, and the in-repo editor frontend (plan
> 03.2.11) owns editor behavior. See the addendum in
> `plans/repository-split-rationale.md`. Kept as a starting point if the
> split is revived.

Full text preserved in git history:
`git show 9a04def:plans/03.1.1-wrapper-setup-plan.md`.
