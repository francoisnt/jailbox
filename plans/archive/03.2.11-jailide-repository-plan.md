# 3.2.11. JailIDE repository, installation, and gates

> Deferred, September 2026. The repository split is deferred: the machine
> boundary is built inside one repository, and the editor workflow becomes an
> in-repo frontend layer over the public CLI (active plan 03.2.11,
> `03.2.11-editor-frontend-plan.md`). See the addendum in
> `plans/repository-split-rationale.md` for the revival triggers. Kept as a
> starting point if the split is revived.

Full text preserved in git history:
`git show 9a04def:plans/03.2.11-jailide-repository-plan.md`.
