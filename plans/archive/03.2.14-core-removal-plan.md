# 3.2.14. Remove extracted jailbox surfaces

> Deferred, September 2026. With the split deferred, config-file and editor
> surfaces are not removed from the repository; they move into the frontend
> layer (active plan 03.2.11). The one piece carried forward is the
> `ssh-config` deletion, superseded by `connection-info`, which the frontend
> plan owns. Kept as a starting point if the split is revived.

Full text preserved in git history:
`git show 9a04def:plans/03.2.14-core-removal-plan.md`.
