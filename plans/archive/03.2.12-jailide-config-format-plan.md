# 3.2.12. `jailide.toml` format and `jailide init`

> Deferred, September 2026. The repository split and the Go/TOML frontend are
> deferred; the in-repo frontend (active plan 03.2.11) keeps the strict
> `jailbox.conf` grammar and `init`. See the addendum in
> `plans/repository-split-rationale.md`. Kept as a starting point if the
> split is revived.

Full text preserved in git history:
`git show 9a04def:plans/03.2.12-jailide-config-format-plan.md`.
