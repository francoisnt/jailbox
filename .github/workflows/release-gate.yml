name: Release Gate

on:
  workflow_dispatch:
    inputs:
      os:
        description: "OS to test on"
        required: false
        default: all
        type: choice
        options:
          - all
          - ubuntu
          - macos
  workflow_call:

jobs:
  portable-smoke:
    name: portable smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    if: >-
      github.event_name != 'workflow_dispatch' ||
      inputs.os == 'all' ||
      (inputs.os == 'ubuntu' && startsWith(matrix.os, 'ubuntu')) ||
      (inputs.os == 'macos' && startsWith(matrix.os, 'macos'))
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Run portable smoke suite
        run: bash tests/portable/smoke.sh

  core-tests:
    name: core tests (${{ matrix.host.os }})
    runs-on: ${{ matrix.host.os }}
    timeout-minutes: 90
    if: >-
      github.event_name != 'workflow_dispatch' ||
      inputs.os == 'all' ||
      (inputs.os == 'ubuntu' && startsWith(matrix.host.os, 'ubuntu')) ||
      (inputs.os == 'macos' && startsWith(matrix.host.os, 'macos'))
    strategy:
      fail-fast: false
      matrix:
        host:
          - os: ubuntu-24.04
            setup_script: tests/ci/setup-linux.sh
          - os: macos-latest
            setup_script: tests/ci/setup-macos.sh
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up host
        run: bash "${{ matrix.host.setup_script }}"

      - name: Lint shell scripts
        run: bash scripts/lint.sh

      - name: Run core test suite
        run: bash tests/run --core-tests

  editor-smoke:
    name: editor smoke (${{ matrix.host.os }}, ${{ matrix.editor_cli }})
    runs-on: ${{ matrix.host.os }}
    timeout-minutes: 60
    if: >-
      github.event_name != 'workflow_dispatch' ||
      inputs.os == 'all' ||
      (inputs.os == 'ubuntu' && startsWith(matrix.host.os, 'ubuntu')) ||
      (inputs.os == 'macos' && startsWith(matrix.host.os, 'macos'))
    strategy:
      fail-fast: false
      matrix:
        host:
          - os: ubuntu-24.04
            setup_script: tests/ci/setup-linux.sh
          - os: macos-latest
            setup_script: tests/ci/setup-macos.sh
        editor_cli:
          - code
          - codium
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up host with editors
        run: bash "${{ matrix.host.setup_script }}" --with-editors

      - name: Build integration test images
        run: bash tests/integration/wrapper-images.sh

      - name: Run editor smoke
        env:
          JAILBOX_EDITOR: ${{ matrix.editor_cli }}
          JAILBOX_EDITOR_TIMEOUT: "60"
        run: bash tests/run --editor-smoke
